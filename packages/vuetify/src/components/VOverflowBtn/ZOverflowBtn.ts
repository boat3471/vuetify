import VOverflowBtn from './VOverflowBtn'
import mixins from '../../util/mixins'

const ZOverflowBtn = mixins(VOverflowBtn).extend({
  name: 'z-overflow-btn',
})

export { ZOverflowBtn }
export default ZOverflowBtn
