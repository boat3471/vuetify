import VChipGroup from './VChipGroup'
import mixins from '../../util/mixins'

const ZChipGroup = mixins(VChipGroup).extend({
  name: 'z-chip-group',
})

export { ZChipGroup }
export default ZChipGroup
