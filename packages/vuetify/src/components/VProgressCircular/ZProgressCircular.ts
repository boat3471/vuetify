import VProgressCircular from './VProgressCircular'
import mixins from '../../util/mixins'

const ZProgressCircular = mixins(VProgressCircular).extend({
  name: 'z-progress-circular',
})

export { ZProgressCircular }
export default ZProgressCircular
