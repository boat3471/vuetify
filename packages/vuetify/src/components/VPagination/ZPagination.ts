import VPagination from './VPagination'
import mixins from '../../util/mixins'

const ZPagination = mixins(VPagination).extend({
  name: 'z-pagination',
})

export { ZPagination }
export default ZPagination
